name: Bridge report
description: Report a MifBridge endpoint that fails, hangs, or reports success while doing something else
title: '[bridge] '
labels: [bridge-report]
body:
  - type: markdown
    attributes:
      value: |
        Reports on this label may be picked up and worked automatically.

        Two things make that safe, and both need your help:

        1. The JSON block below is the ONLY part that ever gets replayed. Your notes are read by a person and never run.
        2. Asset paths you name are moved into a scratch folder before anything runs, so your own
           assets are never opened. That means the repro tests the SHAPE of the bug. If a bug only
           happens with one particular asset, say so in the notes, because that one needs a person.
  - type: textarea
    id: report
    attributes:
      label: The report
      description: Exactly one fenced json block. endpoint, payload, expected and actual are required.
      value: |
        ```json
        {
          "endpoint": "set_spline_points",
          "payload": {"actorPath": "/Game/Your/BP_Thing.BP_Thing", "points": [{"x": 0, "y": 0, "z": 0}]},
          "expected": "what you expected to happen",
          "actual": "what actually happened",
          "dll": "the built <date> string from self_audit",
          "notes": "anything else; read by a person and never run",
          "discord": "your Discord user id, optional; you get pinged when this is fixed"
        }
        ```
    validations:
      required: true
